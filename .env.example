# =============================================
# Service Configuration
# =============================================
SERVICE_ENVIRONMENT=production
SERVICE_HOST=0.0.0.0
SERVICE_PORT=8000
# Number of worker processes. Leave empty to auto-calculate based on CPU cores (recommended).
SERVICE_WORKERS=
SERVICE_LOG_LEVEL=INFO
# A comma-separated list of allowed origins for CORS. Use "*" for all.
SERVICE_CORS_ORIGINS="*"

# =============================================
# Gemini API Configuration - [REQUIRED]
# =============================================
# ‼️ [Required] Gemini API keys, separated by commas, without quotes.
GEMINI_API_KEYS=your-google-ai-api-key-1,your-google-ai-api-key-2

# Cooling period in seconds for a temporarily failed Gemini key.
GEMINI_COOLING_PERIOD=300
# Timeout in seconds for Gemini API requests.
GEMINI_REQUEST_TIMEOUT=120
# Max failure count for a key before being marked as 'failed' permanently. This is NOT a per-request retry.
GEMINI_MAX_RETRIES=3

# =============================================
# Security Configuration - [REQUIRED]
# =============================================
# ‼️ [Required] Client API keys to access the adapter.
# To generate: openssl rand -hex 32
SECURITY_ADAPTER_API_KEYS=your-secure-client-key

# (Recommended) Admin API keys for accessing management endpoints.
SECURITY_ADMIN_API_KEYS=your-secure-admin-key

# =============================================
# Optional: Performance / Caching
# =============================================
# Enable response caching for non-streaming, non-tool requests.
CACHE_ENABLED=True
# Maximum number of items in the cache.
CACHE_MAX_SIZE=1000
# Cache Time-To-Live (TTL) in seconds.
CACHE_TTL=300
# Prefix for all cache keys.
CACHE_KEY_PREFIX=gemini_adapter
